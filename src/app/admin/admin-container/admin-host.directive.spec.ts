import { AdminHostDirective } from './admin-host.directive';

describe('AdminHostDirective', () => {
   it('should create an instance', () => {
      // const directive = new AdminHostDirective();
      // expect(directive).toBeTruthy();
   });
});
